// The game runs entirely inside the renderer as a self-contained WebGL scene.
// No privileged APIs need to be exposed to it, so this preload stays empty
// on purpose (contextIsolation stays on, nodeIntegration stays off).
